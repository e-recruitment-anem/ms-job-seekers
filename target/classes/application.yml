server:
  port: 8090
spring:
  application:
    name: ms-job-seekers
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/ms-job-seekers
    username: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
